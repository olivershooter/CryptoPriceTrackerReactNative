import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    color: "white",
    fontWeight: "bold",
    fontSize: 16,
  },
  ticker: {
    color: "gray",
    fontWeight: "600",
  },
  coinContainer: {
    flexDirection: "row",
  },
});

export default styles;
